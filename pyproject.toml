[project]
name = "ml-experiments"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.14.0",
    "dotenv>=0.9.9",
    "numpy>=2.4.6",
    "torch>=2.12.0",
    "transformers>=5.12.0",
]
